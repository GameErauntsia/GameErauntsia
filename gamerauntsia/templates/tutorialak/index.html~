{% extends "base/index.html" %}
{% load breadcrumbs tutoreus %}

{%block subheader%}
<div id="breadcrumb">{{ request.get_full_path|breadcrumbs }}</div>
<h2>Tutorialak</h2>
<hr/>
{%endblock%}

{% block content %}
<div id="tutoIndexBox" class="inline">


<div id="tutoIndex" class="inline">
{%for app in aplikazioak%}
<a href="aplikazioak/{{app.izena}}" ><img src="{{app.logoa.get_icon_url}}" title="{{app.logoa.title}}" alt="{{app.logoa.caption}}"/></a>
{%endfor%}
<h3>Zure Aplikazioa</h3>
</div>

<div id="gaiaIndex" class="inline">
{%for gaia in gaiak%}
<a href="gaiak/{{gaia.izena}}" ><img src="{{gaia.irudia.get_icon_url}}" title="{{gaia.irudia.title}}" alt="{{gaia.irudia.caption}}"/></a>
{%endfor%}
<h3>Zure Gaia</h3>
</div>

</div>
{% endblock %}
{%block submenu%}
    {% top_tutorialak %}
{%endblock%}